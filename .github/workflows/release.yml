name: Release ZIP

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build ZIP
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          mkdir -p dist/hn-image-optimizer
          rsync -a \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='dist' \
            --exclude='*.zip' \
            . dist/hn-image-optimizer/
          cd dist
          zip -r "../hn-image-optimizer-${VERSION}.zip" hn-image-optimizer/
          cd ..
          echo "ZIP=hn-image-optimizer-${VERSION}.zip" >> "$GITHUB_ENV"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ZIP }}
          generate_release_notes: true
